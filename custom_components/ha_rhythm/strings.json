{
  "config": {
    "step": {
      "user": {
        "title": "HA Rhythm Setup",
        "description": "HA Rhythm analyzes your Home Assistant history and suggests automations based on observed behavioral patterns. Choose an AI provider to generate automation suggestions.",
        "data": {
          "ai_provider": "AI Provider",
          "ai_key": "API Key",
          "ai_base_url": "Base URL (optional override)",
          "ai_model": "Model (optional override)",
          "scan_days": "Days of history to analyze"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only one instance of HA Rhythm is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA Rhythm Options",
        "data": {
          "ai_provider": "AI Provider",
          "ai_key": "API Key",
          "ai_base_url": "Base URL (optional override)",
          "ai_model": "Model (optional override)",
          "scan_days": "Days of history to analyze"
        }
      }
    }
  }
}
